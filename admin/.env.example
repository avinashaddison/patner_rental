# ============================================================================
# Companion Ranchi — Admin Panel (Next.js) environment
# Copy to .env.local and adjust. Only NEXT_PUBLIC_* vars reach the browser.
# ============================================================================

# Base URL of the backend API. The admin client calls ${NEXT_PUBLIC_API_BASE_URL}/api/admin/*
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
